const {expect} = require('chai')

describe('index test', () => {
    it('item test1', () => {
        expect(1+1).to.be.equal(2);
    })

    it('item test2', () => {
        expect(1+1).to.be.equal(3);
    })
})